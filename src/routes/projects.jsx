export default function Projects() {
    return (
        <main>
            <h2>Projects</h2>
            <p class="projects">Currently, there are no projects. Check back again soon.</p>
        </main>
    );
}